name: Apply action control host

on:
  push:
    branches:
      - dev/mo-ai-station
    paths:
      - scripts/apply-action-control-host.js
      - test/control-action-host-wiring.test.js
      - .github/workflows/apply-action-control-host.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout branch
        uses: actions/checkout@v6
        with:
          ref: dev/mo-ai-station

      - name: Verify fail-closed patch contract
        run: node test/control-action-host-wiring.test.js

      - name: Apply fail-closed host patch
        run: node scripts/apply-action-control-host.js

      - name: Verify focused action contracts
        run: |
          node test/control-action-view.test.js
          node test/control-action-http.test.js

      - name: Commit host wiring
        run: |
          if git diff --quiet -- sidecar/index.js; then
            echo "Action control host wiring already present."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add sidecar/index.js
          git commit -m "feat: wire read-only action control endpoint"
          git push origin HEAD:dev/mo-ai-station
